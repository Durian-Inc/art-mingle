import React from "react";
import styled from "styled-components";

const HomePage = styled.main`
  background: blue;
`;

const Home = () => {
  return (
    <HomePage>
      Homepage content bois!
    </HomePage>
  );
}